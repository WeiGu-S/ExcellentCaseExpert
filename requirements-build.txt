# 构建和打包依赖

# PyInstaller - 应用打包
pyinstaller>=5.13.0

# UPX - 可执行文件压缩（可选）
# 注意：需要单独安装UPX二进制文件

# 构建工具
wheel>=0.41.0
setuptools>=68.0.0

# 版本管理
bump2version>=1.0.1

# 文档生成
sphinx>=7.1.0
sphinx-rtd-theme>=1.3.0

# 代码签名工具（可选）
# signtool (Windows)
# codesign (macOS)